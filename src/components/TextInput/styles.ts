import styled from "styled-components";

export const Input = styled.input`
  color: #fff;
  background-color: rgba(0, 0, 0, 0.2);
  border: none;
  outline: none;
  font-size: 1.3rem;
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 1rem;
  margin-top: 0.3rem;
  width: 100%;
`;
